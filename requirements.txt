# Core pipeline dependencies (added stage by stage as we need them)

# Config
PyYAML>=6.0
python-dotenv>=1.0     # loads .env locally; in CI/prod DATABASE_URL is a real env var / secret

# Stage 2 (Ingestion)
yfinance>=0.2.40      # Yahoo Finance data source (B3 tickers use the .SA suffix)
pandas>=2.0           # tabular data handling

# Stage 3 (Storage) — a real hosted Postgres warehouse (Neon/Supabase), not a local file
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Stage 4 (Serving / API)
fastapi>=0.115
uvicorn[standard]>=0.30

# Client-side (notebook) — talks to the API, never to the database directly
requests>=2.31
matplotlib>=3.8

# Tests
pytest>=8.0
httpx>=0.27           # required by fastapi.testclient.TestClient

# Dev tooling (not needed to run the pipeline — only to regenerate notebooks/explore.ipynb)
nbformat>=5.10

# (later stages will add: jinja2 for report templating)
